export const headerHeight = 90

const header = {
  position: 'absolute',
  top: 0,
  width: '100%',
  height: headerHeight,
  alignItems: 'center',
  justifyContent: 'center',
  backdropFilter: 'blur(2.5px)',
}

const headerLogo = {
  color: '#887DC1',
  fontSize: 20,
}

export const styles = {
  header,
  headerLogo,
}
